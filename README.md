# Coffeecash

Ensure you have virtual environment installed:

```
pip3 install virtualenv
python3 -m venv <myenvname> 
```
and activated (on Linux):
```
source <myenvname>/bin/activate
```
If the virtual environment is active, install requirements:
```
pip3 install -r requirements.txt
```
